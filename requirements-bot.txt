# Runtime závislosti cTrader bota (bot_ctrader.py).
# requirements.txt je pre backtest/lab (pandas, matplotlib…) — na serveri
# ich netreba, preto samostatný, minimálny súbor.
ctrader-open-api==0.9.2
protobuf==3.20.1
python-dotenv>=1.0.0

# Twisted (cez ctrader-open-api) bez service-identity robí len rudimentárnu
# kontrolu TLS hostname — na spojení k brokerovi to nechceme.
service-identity>=24.1.0
